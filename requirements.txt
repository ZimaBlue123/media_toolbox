# Core dependencies for Media Toolbox
# model2_image_location requirements
Pillow>=9.0.0
geopy>=2.2.0
piexif>=1.1.3

# (Optional) model1_video_repair can be installed via:
#   pip install -e ./model1_video_repair
